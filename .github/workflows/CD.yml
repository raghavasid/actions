name: CD Workflow

on:
  workflow_run:
    workflows: ["Python CI Workflow"]
    types:
      - completed

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Deploy
        run: echo "Deploying application..."
